#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int a = 0;

    if (a == 0)
    {
        printf("a is zero\n");
    }

    printf("End of program\n");

    return (0);
}
